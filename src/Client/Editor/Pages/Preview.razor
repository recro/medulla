@page "/preview"
@using Medulla.Client.Editor.Library.Abstractions.Environment

<EditorView EditorViewNode="GetPageEditorViewNode()" />


@code {

    private EditorViewNode GetPageEditorViewNode()
    {
        Page page = Page.GetInstance();
        return page.EditorViewNode;
    }
    
}
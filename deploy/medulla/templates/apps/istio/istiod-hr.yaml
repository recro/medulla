---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: istiod
  namespace: istio-system
spec:
  interval: 5m
  chart:
    spec:
      chart: istiod
      version: '1.14.0'
      sourceRef:
        kind: HelmRepository
        name: istiod
        namespace: istio-system
      interval: 1m
  dependsOn:
    - name: base
  upgrade:
    remediation:
      remediateLastFailure: true
  test:
    enable: true
  values:
  
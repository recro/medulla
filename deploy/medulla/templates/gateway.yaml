apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: medulla-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "medulla.io"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: medulla
spec:
  hosts:
  - "medulla.io"
  gateways:
  - medulla-gateway
  http:
  - route:
    - destination:
        host: medulla
        port:
          number: 8000